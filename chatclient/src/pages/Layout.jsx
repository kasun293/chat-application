
/* eslint-disable react/prop-types */
const Layout = ({children}) => {
  return (
    <div style={{width: "100vw", height: "100vh", backgroundColor: "#F5F6FF"}}>
      {children}
    </div>
  )
}

export default Layout
